# HiveHand Vector DB

This directory will hold the local vector index for role work folders:

- `work_folders/phemi/**`
- `work_folders/sima/**`
- `work_folders/leroy/**`

Backend: local (SQLite-based) index written by HiveHand tooling.
Embeddings: Ollama `nomic-embed-text` via http://127.0.0.1:11434.
